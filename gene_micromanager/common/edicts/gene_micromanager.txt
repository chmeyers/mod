

gene_micromanager = {
	length = -1
	icon = "GFX_edict_type_time"
	resources = {
		category = edicts
		upkeep = {
			unity = 1
			multiplier = value:edict_organic_pop_size_effect
		}
	}

	modifier = {
		terraform_speed_mult = 0.50
	}

	potential = {
		OR = {
			has_active_tradition = tr_genetics_finish
			has_active_tradition = tr_genetics_finish_extra_traits
		}
	}

	effect = {
		# Kick it off for the first time.
		# The event loop will check that has_edict = gene_micromanager
		# and disable itself if the edict is turned off.
		country_event = { id = gene_micromanager_mod.1 }
	}

	allow = {}

	ai_weight = {
		weight = 0
	}
}